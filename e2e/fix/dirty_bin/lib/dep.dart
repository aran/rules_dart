// The closure half of the fixture. It is already clean, so its absence from
// the manifest is what says `dart_fix` fixed the entrypoint specifically rather
// than everything it staged.
String binLabel() => 'bin';
