/// Formatted for a 100-column page, not the stock 80: the signature below is
/// what the check disagrees about when `options` is not honoured.
String describeMeasurement(String alpha, String beta, String gamma, String delta) =>
    '$alpha$beta$gamma$delta';
