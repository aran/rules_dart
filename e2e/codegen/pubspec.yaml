name: codegen_e2e
environment:
  sdk: ^3.11.0
dev_dependencies:
  # Holds the shared transitive graph on dart/ext's analyzer line —
  # drift_dev caps analyzer <14, and cross-lock versions must match.
  analyzer: ^13.0.0
  package_config: ^2.2.0
  test: ^1.31.1
