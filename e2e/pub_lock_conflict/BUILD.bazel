load("@rules_dart//dart:defs.bzl", "dart_binary")

exports_files([
    "lock_a.lock",
    "lock_b.lock",
])

# This workspace should FAIL to build due to a version conflict:
# lock_a has path 1.9.1, lock_b has path 1.9.0, and neither
# sets on_version_conflict = "upgrade".
dart_binary(
    name = "app",
    main = "main.dart",
    deps = [
        "@deps_a//:path",
        "@deps_b//:collection",
    ],
)
