load("@buildifier_prebuilt//:rules.bzl", "buildifier")

buildifier(
    name = "buildifier.check",
    exclude_patterns = [
        "./.claude/*",
        "./.git/*",
        "./references/*",
    ],
    lint_mode = "warn",
    mode = "diff",
)
