load("//dart:defs.bzl", "dart_binary")

# Shim binary for the go_router_builder Builder. SharedPartBuilder; produces
# .go_router.g.part shards.
dart_binary(
    name = "shim",
    srcs = ["shim.dart"],
    main = "shim.dart",
    visibility = ["//visibility:public"],
    deps = [
        "//dart/ext:builder_shim",
        "@ext_pub_deps//:go_router_builder",
    ],
)
