load("@bazelrc-preset.bzl", "bazelrc_preset")

bazelrc_preset(
    name = "preset",
    doc_link_template = "https://registry.build/flag/bazel?filter={flag}",
)

# The dual-build collision checker moved to //dart/ext/tools:check_dual_build_collisions
# (Dart port; reads the same baseline extension list Gazelle's filter uses).
